server:
  port: ${PORT:8080}

spring:
  data:
    mongodb:
      uri: mongodb+srv://thanhnghiat42_db_user:yqpvwPOczWuExr0J@coursedb.oind0z4.mongodb.net/?appName=courseDb
      database: courseDb
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}

  mail:
    host: smtp.gmail.com
    port: 587
    username: seanpaul1402@gmail.com
    password: "sfzu mcfd qwdm gtcb"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

  web:
    resources:
      static-locations: classpath:/static/,file:uploads/

mailboxlayer:
  api:
    key: deff4bf40420625187bad6720d63aee3

jwt:
  secret: shapio-super-secret-key-abcxyz1234567890

app:
  upload-dir: uploads/

# ============================================
# PayOS Payment Configuration
# ============================================
payos:
  client-id: ${PAYOS_CLIENT_ID:20f87798-7fcc-4c38-9ad8-2db958f5e806}
  api-key: ${PAYOS_API_KEY:4ec884f6-d69d-44ce-b69b-0277ffcd28cc}
  checksum-key: ${PAYOS_CHECKSUM_KEY:945e5605eb1eb9be573fa0d1733aca3bbe9ebe834fa0993d52cc81455613a209}

  # Endpoints
  api-base-url: ${PAYOS_API_BASE_URL:https://api-merchant.payos.vn}

  # Frontend base URL for user redirect after PayOS return/cancel
  frontend-base-url: ${FRONTEND_BASE_URL:http://localhost:5173}

  # Return/cancel must point to BACKEND endpoints in Option B (BE will redirect to FE)
  return-url: ${PAYOS_RETURN_URL:http://localhost:8080/api/payment/payos/return}
  cancel-url: ${PAYOS_CANCEL_URL:http://localhost:8080/api/payment/payos/cancel}

  # Webhook must be reachable by PayOS server; keep configurable per env
  webhook-url: ${PAYOS_WEBHOOK_URL:http://localhost:8080/api/payment/payos/webhook}
